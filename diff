diff --git a/README.md b/README.md
index 3f34295..0143f7a 100644
--- a/README.md
+++ b/README.md
@@ -5,19 +5,13 @@ A variant of [ArchLabs](https://bitbucket.org/archlabslinux/iso)
 
 ### Building the ISO
 
-The package `archiso` must be installed and it needs to be built on an `Arch x86_64` system.
+`sudo pacman -S archiso mkinitcpio-archiso git squashfs-tools --needed`
 
-First fork or clone the repo to your system.
+`git clone https://gitlab.com/syncopatedlinux/iso.git ~/Workspace/iso`
 
-    git clone https://gitlab.com/syncopatedlinux/iso.git ~/Workspace/iso
+`pacman -Scc --noconfirm`
 
-Before building you will need to clean your pacman cache.
-
-    pacman -Scc
-
-Now just run the build script with `-u` to update the installer, see `./build -h` for more options.
-
-    ./build -u
+`./build -u`
 
 
 When finished there will be a directory called `out`, the ISO will be in there.
@@ -34,11 +28,10 @@ When finished there will be a directory called `out`, the ISO will be in there.
 
 - `pacman.conf` is used while building and has entries for the `archlabs` and `archlabs-testing` repos.
 
-- `airootfs` is the live boot file system. This is where to add anything that you want included on the ISO.  
+- `airootfs` is the live boot file system. This is where to add anything that you want included on the ISO.
 
-Remember, **everything must be done as root**, if you add something, do it with `su`, `sudo`, or `doas`.  
-Once added update `profiledef.sh` if special permissions are needed *(executable, read-only, etc.)*  
-Also note that the files included in the iso will **not** be added to installed systems, this must be done  
+Remember, **everything must be done as root**, if you add something, do it with `su`, `sudo`, or `doas`.
+Once added update `profiledef.sh` if special permissions are needed *(executable, read-only, etc.)*
+Also note that the files included in the iso will **not** be added to installed systems, this must be done
 in the archlabs installer.
 
-When finished, run the `build` script in the main directory.
diff --git a/airootfs/etc/pacman.conf b/airootfs/etc/pacman.conf
index 6322880..044d4db 100644
--- a/airootfs/etc/pacman.conf
+++ b/airootfs/etc/pacman.conf
@@ -73,8 +73,9 @@ LocalFileSigLevel = Optional
 #[testing]
 #Include = /etc/pacman.d/mirrorlist
 
-# [syncopated]
-# Server = http://soundbot.hopto.org/packages/archlinux/x86_64/
+[syncopated]
+SigLevel = Optional TrustAll
+Server = http://soundbot.hopto.org/packages/archlinux/x86_64/
 
 [proaudio]
 SigLevel = Optional TrustAll
@@ -101,6 +102,10 @@ Include = /etc/pacman.d/mirrorlist
 [multilib]
 Include = /etc/pacman.d/mirrorlist
 
+[cachyos]
+SigLevel = Optional TrustAll
+Server = https://mirror.cachyos.org/repo/$arch/$repo
+
 # [archlabs-testing]
 # Server =  https://pub-7d84e047b852442a86fd6d7feb1ff2cd.r2.dev/$repo/$arch
 # Server =  https://github.com/ArchLabs/$repo/raw/master/$arch
diff --git a/airootfs/usr/local/bin/installer b/airootfs/usr/local/bin/installer
index 02691b1..1c6c127 100755
--- a/airootfs/usr/local/bin/installer
+++ b/airootfs/usr/local/bin/installer
@@ -2049,7 +2049,7 @@ install_cachyos_repos() {
   done
 }
 
-install_cachyos_keys() {
+install_cachyos_key() {
     echo "Installing CachyOS repo.."
 
 	{
@@ -2075,9 +2075,19 @@ install_cachyos_keys() {
 install_osamc_key() {
 	chrun "curl https://arch.osamc.de/proaudio/osamc.gpg | pacman-key --add -"
 
+	chrun "pacman-key --finger 762AE5DB2B38786364BD81C4B9141BCC62D38EE5"
+
 	chrun "pacman-key --lsign-key 762AE5DB2B38786364BD81C4B9141BCC62D38EE5"
 }
 
+install_syncopated_key() {
+	chrun "curl http://soundbot.hopto.org/syncopated.gpg | pacman-key --add -"
+
+	chrun "pacman-key --finger 36A6ECD355DB42B296C0CEE2157CA2FC56ECC96A"
+
+	chrun "pacman-key --lsign-key 36A6ECD355DB42B296C0CEE2157CA2FC56ECC96A"
+}
+
 install_check_bg()
 {
 	# check for errors in the background install
@@ -2105,11 +2115,13 @@ install_check_bg()
 			chrun "pacman-key --finger '$key'"
 			chrun "pacman-key --lsign-key '$key'"
 		fi
+
 		chrun "pacman -Syyu --noconfirm"
 		chrun "pacman -S archlinux-keyring archlabs-keyring --noconfirm"
 
+		install_cachyos_key
 		install_osamc_key
-		install_cachyos_keys
+		install_syncopated_key
 
 		chrun "pacman -S ${ISO_PKGS[*]} $luks $NET_TYPE --noconfirm --needed"
 
@@ -2260,7 +2272,7 @@ install_background()
 			fi
 			chrun "pacman -S archlinux-keyring archlabs-keyring --noconfirm"
 
-			install_cachyos_keys
+			install_cachyos_key
 
 			chrun "pacman -S ${ISO_PKGS[*]} $luks $NET_TYPE --noconfirm --needed"
 
diff --git a/pacman.conf b/pacman.conf
index 8f315a6..9a989f8 100644
--- a/pacman.conf
+++ b/pacman.conf
@@ -5,12 +5,9 @@ ParallelDownloads = 5
 SigLevel          = Required DatabaseOptional
 LocalFileSigLevel = Optional
 
-[cachyos]
+[syncopated]
 SigLevel = Optional TrustAll
-Server = https://mirror.cachyos.org/repo/$arch/$repo
-
-# [syncopated]
-# Server = http://soundbot.hopto.org/packages/archlinux/x86_64/
+Server = http://soundbot.hopto.org/packages/archlinux/x86_64/
 
 [proaudio]
 SigLevel = Optional TrustAll
@@ -28,6 +25,10 @@ Include = /etc/pacman.d/mirrorlist
 [multilib]
 Include = /etc/pacman.d/mirrorlist
 
+[cachyos]
+SigLevel = Optional TrustAll
+Server = https://mirror.cachyos.org/repo/$arch/$repo
+
 # [archlabs-testing]
 # SigLevel =  Optional TrustAll
 # Server =  https://pub-7d84e047b852442a86fd6d7feb1ff2cd.r2.dev/$repo/$arch
